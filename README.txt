Plik zotsał stworzony lokalnie(mongodb://127.0.0.1:27017/idosell), ponieważ był problem z odczytywaniem rekordu SRV, endpointy REST API są zabezpieczone JWT, CSV jest obsługiwany, filtrowanie minWorth, maxWorth jest. plik sample jest, ponieważ bramki dają error 404, przez co jest zero zamówień, lecz opcja z nimi jest w pliku po prostu zahaszowana. ENV zostawiony aby pan miał dostęp do jego danych.